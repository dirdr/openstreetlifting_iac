# ==============================================================================
# OpenStreetLifting - Production Deployment Configuration
# ==============================================================================
# ⚠️  SECURITY WARNING: This file contains sensitive configuration.
#     Copy this file to .env and update with secure production values.
#     NEVER commit .env file to version control.
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# PostgreSQL connection settings
# ⚠️  CHANGE THESE IN PRODUCTION - Use strong, randomly generated passwords
DB_USER=appuser
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION
DB_NAME=appdb

# Database host port mapping (host:container)
# Default: 5432 (PostgreSQL standard port)
DB_PORT=5432

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
# Application identifier (used in Traefik routing labels)
APP_NAME=openstreetlifting

# Server configuration (container internal settings)
# These values are typically standard and don't need changing
HOST=0.0.0.0
PORT=8080

# ------------------------------------------------------------------------------
# Authentication
# ------------------------------------------------------------------------------
# API_KEYS: Comma-separated list of valid API keys for request authentication
# ⚠️  REQUIRED IN PRODUCTION - Generate secure random keys
# Example: openssl rand -hex 32
API_KEYS=CHANGE_ME_IN_PRODUCTION
